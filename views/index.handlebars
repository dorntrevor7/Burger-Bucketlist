<h1>LET'S EAT SOME BURGERS!</h1>

<h2>Eaten Burgers</h2>

<ul>
  {{#each burgers}}
    {{#unless devour}}
      {{> burgers/buger-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers still on my bucketlist</h2>

<ul>
  {{#each burgers}}
    {{#if devour}}
      {{> burgers/burger-block devour=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Type:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devour">Eaten it?</label><br>
    <input type="radio" name="devour" value="1" checked> Eaten!<br>
    <input type="radio" name="devour" value="0"> Not yet!
  </div>

  <button type="submit">Add Burger</button>
</form>
